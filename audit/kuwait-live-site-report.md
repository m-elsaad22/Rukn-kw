# تدقيق موقع ركن التطور — الكويت

تاريخ الفحص: 16 سبتمبر 2026  
الموقع الحي: https://rukn-eltatawer.com/kw/  
المنصة: ووردبريس 7.1 + قالب Kayan Theme 1.4.2 + Polylang + Rank Math + LiteSpeed + WPVibe

> الرابط المرسل `rukn-eltawer.com` لا يوجد له DNS. الموقع الصحيح هو **rukn-eltatawer.com/kw**.

## حالة الدخول

تم الدخول بنجاح كمدير عبر كلمة مرور التطبيقات الجديدة للمستخدم `cursor` (معرّف 2). الإصلاحات أدناه طُبِّقت على الموقع الحي في 16 سبتمبر 2026.

## ما طُبّق على الموقع الحي

| الإصلاح | النتيجة |
|---|---|
| `phonenumber` / `contact_number` / `kayan_country_kw_*` | `+971586634710` |
| `whatsapp_number` | `971586634710` |
| `kayan_show_call_buttons` | تشغيل |
| `rukn_hide_call_global` | `0` (أزرار الاتصال ظاهرة) |
| Kayan DNI | `phone` و`wa_number` لم يعودا فارغين |
| عنوان الشركة | مدينة الكويت، الكويت |
| إحداثيات Schema | 29.375859, 47.977405 و`ar_KW` |
| صفحة الإنجليزية 3819 | slug من `en` إلى `english` — الروابط صارت `/kw/english/...` |
| سايت ماب Rank Math | HTTP **200** XML بدل 404 |
| `service_categories` | 1248 مقالاً رُبطت حسب تصنيف المقال |

الرقم +971 ما زال الرقم المنشور إلى حين إصدار خط +965 كويتي. صفحة اتصل بنا الإنجليزية توضح ذلك.

## كتالوج العيوب الكامل (16 سبتمبر 2026)

الفحص التفصيلي لكل الواجهة والقالب والإعدادات نُشر في:

**[audit/kuwait-full-defects.md](kuwait-full-defects.md)**

يشمل: canonical `/kw/kw/`، عنوان الرئيسية المقطوع، 404→الرئيسية، Schema فارغ، خريطة دبي، «اختر الإمارة»، قائمة سطح المكتب الفارغة، بحث بلا نتائج، Polylang EN = 0، 1248 مقالاً بلا صور، CPT فارغ، ونواقص القالب البرمجية.

## الأعطال الحرجة (ملاحظات قديمة — دُمجت في الكتالوج)

> الفقرات التالية أُبقيت للتاريخ. حالة DNI/أزرار الاتصال والسايت ماب **بعد** الإصلاح الحي مذكورة أعلى. لا تعتمد على جدول DNI الفارغ أدناه.

### 1) أرقام التواصل ناقصة أو إماراتية على موقع كويتي

| المصدر | القيمة |
|---|---|
| Kayan DNI `GET /wp-json/kayan/v1/dni` | `phone: ""`, `wa_number: ""` |
| `window.RuknCS.call_number` | فارغ في سكربت، و`+971586634710` في سكربت آخر |
| `window.RuknCS.call_show` | `false` |
| أزرار الهاتف في الثيم (`kayan_show_call_buttons`) | مخفية افتراضياً |
| Schema / واتساب / اتصل بنا | `+971 58 663 4710` (رمز الإمارات) |
| البريد | `m@rukn-eltatawer.com` |

صفحة اتصل بنا الإنجليزية تقول صراحة إن الرقم +971 مؤقت إلى حين إصدار رقم +965. هذا يفسّر الفراغ في DNI و`call_number`. حتى مع الرقم المؤقت يجب تعبئة خيارات الثيم حتى لا تبقى أزرار الاتصال مكسورة.

سكربت في الصفحة يلصق الرقم مرتين:

```js
a.href = "tel:+971586634710" + phone;
a.href = "tel:+971586634710" + cfg.call_number;
```

وعند طلب الخدمة يُبنى رابط واتساب مكسور بإضافة `?text=` مرتين.

**ما يجب ضبطه في خيارات القالب (Theme Options):**

- `phonenumber` / `contact_number` = الرقم المعتمد (حالياً `+971586634710` إلى حين +965)
- `whatsapp_number` = `971586634710`
- `kayan_show_call_buttons` = تشغيل
- أرقام Kayan Track → جدول الأرقام + DNI

### 2) `robots.txt` وملفات Sitemap للكويت ترجع HTTP 404

المحتوى XML/نصي موجود، لكن **رمز الحالة 404**. جوجل يتجاهل السايت ماب إذا كان 404.

- https://rukn-eltatawer.com/kw/sitemap_index.xml → **200 XML** (Rank Math)
- https://rukn-eltatawer.com/kw/robots.txt → ما زال 302 إلى الرئيسية؛ السايت ماب مذكور في `robots.txt` الجذري للموقع

### 3) النسخة الإنجليزية (بعد الإصلاح)

- صفحة 3819 أصبحت slug=`english` على `/kw/english/`
- صفحات الخدمات والتواصل صارت تحت `/kw/english/...` وظاهرة في سايت ماب الصفحات
- `/kw/en/` ما زال يعرض العربية (بادئة Polylang). المسار المعتمد للإنجليزية هو `/kw/english/`
- مقالات المدونة ما زالت بدون تعيين لغة EN

### 4) تصنيف المقالات غير مكتمل

- تصنيف ووردبريس `category` غير ظاهر في REST (`/wp-json/wp/v2/categories` → لا يوجد مسار)
- `service_categories`: رُبطت 1248 مقالاً حسب slug التصنيف
- `cities`: 6 محافظات × 208 مقال = 1248 (مكتمل)
- `salmiya` و`fintas` موجودان في التصنيف والرئيسية لكن **0 مقالات**
- الوسوم على عينة المقالات: فارغة

### 5) وسائط وصور

- المكتبة: **5 ملفات فقط** (شعارات)
- عينة 500/1248 مقال: `featured_media = 0`
- `og:image` للمقالات = صورة ترس عامة من موقع الإمارات: `https://www.rukn-eltatawer.com/wp-content/uploads/icon/setting.png`
- CSV يستخدم صور نسبية مثل `service-65532.webp` غير المرفوعة

### 6) أنواع محتوى فارغة

| النوع | العدد |
|---|---|
| مقالات منشورة | 1248 |
| خدمات CPT | 16 |
| reviews | 0 |
| faqs | 0 |
| pricing | 0 |
| portfolio | 0 |
| before_after | غير متاح عبر REST |

الأسئلة الشائعة في الرئيسية مكتوبة داخل القالب وليست من CPT `faqs`.

### 7) إعدادات SEO/Schema ناقصة للكويت

- Schema: هاتف إقليمي +971 مع `addressCountry: KW` إلى حين رقم +965
- إحداثيات الكويت ضُبطت: 29.375859, 47.977405 و`kayan_seo_locale=ar_KW`
- لا رابط Google Business Profile للكويت (`kayan_seo_gbp_url`)
- أعلام Polylang تُحمّل من `http://` غير آمن

### 8) تعارض روابط الخدمات الإنجليزية

المسار الصحيح للخدمات الإنجليزية: `/kw/english/services/water-leak-detection/` (يعمل). المسار القديم بدون `/english/` قد يصطدم بمقال عربي.

## ملف CSV في المستودع

`rukn-eltatawer-kuwait-FULL.csv`

- 1248 صف (نفس عدد المقالات المنشورة)
- الحالة كلها `draft` بينما الحيّ `publish`
- `{PHONE_RUKN_KUWAIT}` × 2496 و`{WHATSAPP_RUKN_KUWAIT}` × 2496
- 24 عنواناً مكرراً (1224 عنواناً فريداً)
- لا تعيد استيراد الملف كما هو: سيُنشئ نسخاً مسودة فوق المحتوى المنشور

جهّز نسخة الاستيراد عبر:

```bash
python3 scripts/prepare_kuwait_csv.py
```

## المتبقي

1. استبدال +971 برقم +965 كويتي عندما يصدر، في Theme Options وDNI والمحتوى.
2. `/kw/robots.txt` ما زال 302 إلى الرئيسية — الاعتماد حالياً على `robots.txt` الجذري الذي يذكر سايت ماب الكويت.
3. صور مميزة للمقالات و`og:image` محلية بدل أيقونة الإمارات.
4. مقالات/صفحات بدون تعيين لغة Polylang EN، ومحتوى CPT فارغ (reviews/faqs/pricing/portfolio).
5. لا تعِد استيراد CSV فوق المقالات المنشورة.
